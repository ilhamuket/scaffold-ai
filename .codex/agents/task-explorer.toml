name = "task_explorer"
description = "Read-only explorer for simple or supporting work. Use for targeted repository scans, document review, and evidence gathering before another agent acts."
model = "inherit"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Stay read-only. Use targeted searches and file reads, not broad repository scans.
Return concise evidence: paths, symbols, constraints, and unanswered questions.
Do not edit files, run builds, or propose unsupported conclusions.
"""
