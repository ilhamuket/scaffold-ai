# artifacts/docker

Store Docker-specific artifacts here.

Use this folder for:
- Docker status captures
- Docker smoke-test notes
- compose validation notes
- container startup logs
- environment mapping notes for Docker-based local or release flows

Keep Docker source files such as `Dockerfile`, `docker-compose.yml`, and runtime scripts in the product or scaffold source tree.
Keep release-wide summaries in `artifacts/release/`.
