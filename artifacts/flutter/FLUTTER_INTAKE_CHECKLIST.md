# FLUTTER_INTAKE_CHECKLIST

Use this checklist during `I1 Existing Project Intake` when a target repo appears to be a Flutter/Dart project.

## Metadata
- module: `flutter-intake`
- artifact_type: `intake-checklist`
- created_at: `2026-04-26T21:42:16+07:00`
- updated_at: `2026-04-26T21:42:16+07:00`
- runtime: `codex`
- owner: `founder`
- status: `pending_target_repo`
- task_id: `TASK-20260425-001`
- resume_safe: `yes`

## Detection
- [ ] `pubspec.yaml` found
- [ ] `lib/` found
- [ ] `test/` found
- [ ] `integration_test/` found
- [ ] platform folders found: `android/`, `ios/`, `web/`, `macos/`, `windows/`, `linux/`

## Stack Facts To Collect
- Flutter SDK constraint:
- Dart SDK constraint:
- Flutter channel/version source:
- FVM or SDK pinning:
- package manager/scripts:
- mono-repo tooling:
- lint config:
- generated code tools:

## App Structure
- app root:
- package roots:
- entrypoints:
- main files:
- routing/navigation:
- state management:
- dependency injection:
- API/data layer:
- persistence/storage:
- theming/design system:
- localization:
- assets/fonts:
- environment/flavor setup:

## Platform Targets
- Android:
- iOS:
- Web:
- Desktop:
- minimum supported OS versions:
- permissions:
- platform channels/native integrations:

## Test And QA Stack
- unit tests:
- widget tests:
- integration tests:
- golden tests:
- analyzer/lint command:
- CI test command:
- manual smoke route:
- emulator/device requirements:

## Release And Deployment
- Android build path:
- iOS build path:
- web build path:
- version/build number strategy:
- flavor/build variant:
- signing status:
- store/release ownership:
- rollback path:

## Constraints And Risks
- inherited constraints:
- known failing tests:
- known flaky tests:
- platform risks:
- dependency risks:
- migration risks:
- environment access gaps:

## Output Rules
- Do not mark this checklist complete until a real target repository has been audited.
- Do not store secrets, signing keys, tokens, provisioning profiles, or private credentials.
- Update Feature Registry only from verified intake evidence.
