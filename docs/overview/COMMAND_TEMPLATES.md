# COMMAND_TEMPLATES

Command templates have moved to the root command catalog:

`COMMANDS.md`

Use `COMMANDS.md` as the single source of truth for:
- session commands
- numbered work menu
- onboarding commands
- read-only intake commands
- pre-coding gate commands
- official workflow step commands
- prompt patterns

This file remains only as a compatibility pointer for older references.
