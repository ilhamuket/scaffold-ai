# Master UI Templates

Reusable design templates belong here when the scaffold needs shared UI baselines.

Active feature-specific design belongs in:

```text
dev-doc/[feature-name]/design/
```

Do not store active feature design under `artifacts/`.
